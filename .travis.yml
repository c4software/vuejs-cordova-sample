language: android

android:
  components:
    - build-tools-25.2.3
    - android-25

install:
  - nvm install 5
  - npm cache clean
  - npm install
  - npm install -g cordova

before_script:
  - cordova platform add android

script:
  - cordova prepare
  - cordova build android --release
